{
  "name": "@repo/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot server.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "type-check": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun test --shuffle",
    "test:unit": "bun test --shuffle src/utils/__tests__ src/services/__tests__ --exclude '*.gemini-provider.test.ts'",
    "test:integration": "bun test --shuffle src/routes/__tests__ --exclude '*.contract.test.ts'",
    "test:contract": "bun test --shuffle src/routes/__tests__/*.contract.test.ts",
    "test:gemini": "TEST_TARGETS=real-api bun test --shuffle src/services/__tests__/gemini-provider.test.ts",
    "test:all": "bun test --shuffle"
  },
  "dependencies": {
    "@google/genai": "^1.29.0",
    "@repo/shared": "workspace:*",
    "hono": "^4.0",
    "openai": "^4.0",
    "zod": "^3.23",
    "zod-to-json-schema": "^3.24.6"
  }
}
