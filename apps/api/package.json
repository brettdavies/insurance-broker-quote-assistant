{
  "name": "@repo/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "hono serve src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "type-check": "tsc --noEmit",
    "lint": "biome check .",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.0",
    "openai": "^4.0",
    "zod": "^3.23",
    "@repo/shared": "workspace:*"
  }
}
