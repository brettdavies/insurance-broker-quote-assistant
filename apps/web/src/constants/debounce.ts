/**
 * Debounce Constants
 *
 * Centralized constants for debounce timings in the frontend.
 */

/**
 * Debounce delay for LLM extraction (milliseconds)
 * Wait this long after user stops typing before calling extraction API
 */
export const EXTRACTION_DEBOUNCE_MS = 500
